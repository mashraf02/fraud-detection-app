# Runtime dependencies for the API, training pipeline and migrations.
# Development and test tooling lives in requirements-dev.txt.

# --- API ---
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Persistence ---
SQLAlchemy==2.0.54
alembic==1.20.0
# psycopg 3 is the Postgres driver; the service also runs on SQLite for local
# development and tests.
psycopg[binary]==3.3.6

# --- Modelling ---
scikit-learn==1.9.1
joblib==1.6.0
numpy==2.5.3
pandas==3.0.5
scipy==1.18.1
